import { MD3Theme } from 'react-native-paper';

export const chatScreenStyles = (theme: MD3Theme) => ({
    container: {
        flex: 1 as const,
        backgroundColor: theme.colors.background,
    },
});
